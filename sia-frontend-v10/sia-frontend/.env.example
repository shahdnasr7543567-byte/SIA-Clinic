# Copy this file to .env.local and point it at your backend once it exists.
# NOTE: matches the "baseUrl" variable in the backend's Postman collection
# (http://localhost:5000/api) — the old default here (4000) was a guess.
VITE_API_BASE_URL=http://localhost:5000/api
